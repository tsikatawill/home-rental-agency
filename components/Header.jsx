import { Navbar } from "../components";

export const Header = () => {
  return (
    <header className="h-[790px] bg-[url(/images/header-bg.jpg)] bg-cover bg-no-repeat bg-center">
      <Navbar />

      {/* Hero */}
    </header>
  );
};
